apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-billing-service
  labels:
    app: bookstore-billing-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookstore-billing-service
  template:
    metadata:
      labels:
        app: bookstore-billing-service
    spec:
      containers:
        - name: billing-service
          image: bookstore-billing-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5001
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "mysql"
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: bookstore-db
                  key: url
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: bookstore-db
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: bookstore-db
                  key: password
---
apiVersion: v1
kind: Service
metadata:
  name: bookstore-billing-service
  labels:
    app: bookstore-billing-service
spec:
  type: ClusterIP
  selector:
    app: bookstore-billing-service
  ports:
    - name: http
      port: 5001
      targetPort: 5001
